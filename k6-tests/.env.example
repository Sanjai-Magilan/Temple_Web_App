# k6 Load Test Environment Configuration
# Copy this file to .env and fill in your values
# Usage: source .env && k6 run load-test.js

# Application Configuration
BASE_URL=http://localhost:3002

# Test User Credentials
TEST_USER_EMAIL=sabareesanmaruthamuthu@gmail.com
TEST_USER_PASSWORD=San.com3267

# Admin User Credentials (for admin tests)
TEST_ADMIN_EMAIL=admin@temple.com
TEST_ADMIN_PASSWORD=admin@123

# Database Monitoring (for monitor-db.sh script)
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=templeapp

# Test Configuration
SCENARIO=load          # Options: smoke, load, stress, spike, soak
VUS=10                # Number of virtual users
DURATION=5m           # Test duration

# Additional Options
# TEST_TYPE=authenticated  # For focused-test.js: browsing, authenticated, api-heavy, admin
